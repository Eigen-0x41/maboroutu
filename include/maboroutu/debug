#include <print>
#include <source_location>

#define EXPECT_EQ(val, expect)                                                 \
   if ((val) != (expect)) [[unlikely]]                                         \
   std::println("{}\nvalue is not match. [ {}: {}, expect: {} ]",              \
                std::source_location::current(), #val, val, expect)
